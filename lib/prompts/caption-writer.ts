// Reserved for future split where caption generation runs as a second Claude pass
// (e.g. after human edits the hook). For now, content-decision.ts handles the full post.
export const captionWriterPrompt = "";
